#include <stdio.h>
#include <stdlib.h>

#include "trees.h"

int main(void)
{
    Node *root = create();

    printf("Parcurgerea arborelui in ordine:\n");
    // inorder(root);
    // printf("\n");

    // int a, b;
    // printf("Introduceti doua noduri pentru a gasi LCA: ");
    // scanf("%d %d", &a, &b);

    // Node *lca = findLCA(root, a, b);
    // if (lca)
    // {
    //     printf("LCA este: %d\n", lca->data);
    // }

    // freeTree(root);

    return 0;
}